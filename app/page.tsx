export default function Home() {
  // The beforeFiles rewrite in next.config.ts serves the preserved production
  // document. This component is a build-time fallback required by Next.js.
  return null;
}
